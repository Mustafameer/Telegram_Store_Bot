# دليل البدء السريع - الإصلاحات الجديدة

## 📋 المتطلبات

### للبوت:
- Python 3.8+
- pyTelegramBotAPI
- psycopg2 (لـ PostgreSQL)
- جميع المكتبات المذكورة في requirements.txt

### للتطبيق Desktop:
- Flutter (آخر نسخة)
- Dart SDK
- Windows/Linux/macOS

---

## 🚀 تشغيل البوت

### الطريقة 1: من Terminal
```bash
cd C:\Users\Hp\Desktop\TelegramStoreBot
python bot.py
```

### الطريقة 2: استخدام البرنامج
```bash
.\start_bot.bat
```

### التحقق من التشغيل:
```powershell
Get-Process python | Where-Object {$_.Id -eq 1772} # أو 23828
```

---

## 🎨 تشغيل تطبيق Desktop

### الخطوات:
```bash
cd flutter_store_app
flutter pub get
flutter run -d windows
```

### أو بناء Release:
```bash
flutter build windows
```

---

## ✅ اختبار الميزات الجديدة

### 1. اختبار أسماء العملاء
- توقع: الإشعار يعرض "اسم العميل" من جدول CreditCustomers
- الطريقة: اشترِ من متجر مغلق، تحقق من الإشعار

### 2. اختبار الطلبات المؤكدة
- توقع: لا تظهر في قائمة الطلبات المعلقة
- الطريقة: اشترِ من متجر مغلق، انتظر التأكيد التلقائي

### 3. اختبار الصور
- توقع: تصل صور المنتج للعميل
- الطريقة: اشترِ منتج بصور، تحقق من المرفقات

### 4. اختبار شاشة الرسائل
- توقع: رسائل جديدة تظهر في البوت
- الطريقة: شاهد تبويب الرسائل → "الرسائل"

### 5. اختبار شاشة الطلبات
- توقع: جميع الطلبات تظهر مع التفاصيل
- الطريقة: شاهد تبويب الطلبات → "الطلبات"

### 6. اختبار كشف الحساب
- توقع: رصيد العميل يُخصم من الحساب الآجل
- الطريقة: شاهد التبويب → "إدارة العملاء" → اختر عميل

---

## 🔧 استكشاف الأخطاء

### البوت لا يبدأ
```bash
python -m py_compile bot.py  # تحقق من الأخطاء
```

### الرسائل لا تظهر
- تحقق من اتصال PostgreSQL
- تحقق من وجود بيانات في جدول Messages

### الصور لا تظهر
- تحقق من موجود ملفات الصور في `data/Images/`
- تحقق من جدول imagestorage

---

## 📊 قاعدة البيانات

### البوت يستخدم:
- **Local:** SQLite في `data/store.db`
- **Cloud:** PostgreSQL على Railway (عند وجود `DATABASE_URL`)

### الجداول الرئيسية:
- Messages - الرسائل والإشعارات
- Orders - الطلبات
- CreditCustomers - العملاء الآجليين
- imagestorage - الصور

---

## 📱 حسابات الاختبار

```
معرّف تلغرام: 1041977029
معرّف المتجر (مغلق): 21
معرّف العميل: 30
معرّف المنتج: 4
```

---

## 🎯 سيناريوهات الاختبار

### السيناريو 1: شراء من متجر مغلق
1. شغّل البوت
2. شغّل Desktop App
3. سجل دخولك في البوت
4. انتقل إلى متجر مغلق
5. اشترِ منتج
6. توقع:
   - رسالة في البوت للبائع
   - إشعار بالاسم الصحيح
   - طلب معاد في البوت (Confirmed)
   - طلب في Desktop (لا يظهر في Pending)
   - رسالة في Desktop
   - خصم من حسابك الآجل

### السيناريو 2: شراء من متجر عادي
1. اشترِ من متجر مفتوح
2. توقع:
   - طلب في حالة Pending
   - ينتظر موافقة البائع
   - يعمل بدون خصم آجل (إذا لم يكن عميلاً آجلاً)

---

## 📝 الملفات المعدلة

### Bot:
- ✅ bot.py - 3 إصلاحات رئيسية

### Desktop:
- ✅ postgres_service.dart
- ✅ database_helper_cloud.dart
- ✅ home_screen.dart
- ✅ messages_screen.dart
- ✅ orders_screen.dart (جديد)

---

## 🎓 المزيد من المعلومات

انظر الملفات:
- `COMPLETE_FIXES_REPORT.md` - تقرير شامل
- `BOT_FIXES_APPLIED.md` - تفاصيل الإصلاحات
- `DESKTOP_UPDATES.md` - تحديثات Desktop
- `SESSION_SUMMARY.md` - ملخص الجلسة

---

## 💡 نصائح

1. **استخدم متصفح DevTools** للتطبيق Desktop:
   ```bash
   flutter run -d windows --enable-software-battery-status
   ```

2. **اتصل بـ PostgreSQL مباشرة** للتحقق من البيانات:
   ```bash
   psql -h switchback.proxy.rlwy.net -U postgres -d store
   ```

3. **راقب سجلات البوت** في وقت التشغيل لاكتشاف المشاكل مبكراً

---

## ✨ الحالة النهائية

- ✅ جميع الأخطاء تم حلها
- ✅ جميع الميزات تم تنفيذها
- ✅ الكود جاهز للإنتاج
- ✅ التطبيقات جاهزة للاستخدام

**استمتع باستخدام التطبيق المحسّن!** 🎉

