# 🧪 دليل اختبار تعديلات عرض المنتجات في المتاجر المغلقة

## 📋 المقدمة

هذا الدليل يشرح كيفية اختبار تعديلات عرض المنتجات في المتاجر المغلقة (التي تتطلب تسجيل الزبون).

---

## 🎯 الأهداف

- التحقق من أن المتاجر المفتوحة تعرض صور المنتجات بشكل عادي
- التحقق من أن المتاجر المغلقة تعرض صور للمالك فقط
- التحقق من أن الزبائن المسجلين في متجر مغلق يرون نصوص فقط بدون صور
- التحقق من أن الزبائن غير المسجلين يرون رسالة رفض الدخول

---

## 🏗️ متطلبات الاختبار

### 1. متجر مفتوح (للاختبار)
- الإنشاء: إذا لم يكن موجوداً
- الإعدادات:
  - `require_registration = 0` (مفتوح)
  - وجود منتجات ذات صور
  - الحالة: نشط

### 2. متجر مغلق (للاختبار)
- الإنشاء: إذا لم يكن موجوداً
- الإعدادات:
  - `require_registration = 1` (مغلق)
  - وجود منتجات ذات صور
  - إضافة زبائن مسجلين
  - الحالة: نشط

### 3. حسابات للاختبار
- **حساب 1:** صاحب متجر مفتوح (Telegram ID مختلف)
- **حساب 2:** صاحب متجر مغلق (Telegram ID مختلف)
- **حساب 3:** زبون مسجل في المتجر المغلق (Telegram ID مختلف)
- **حساب 4:** زبون غير مسجل (Telegram ID مختلف)

---

## 🧪 حالات الاختبار

### اختبار 1: المتجر المفتوح - صاحب المتجر

```
الخطوات:
1. تسجيل الدخول بحساب صاحب المتجر المفتوح
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المفتوح الخاص بك
4. تصفح المنتجات

النتيجة المتوقعة:
✅ عرض المنتجات مع الصور
✅ عرض الأزرار (إضافة للسلة، الكمية)
```

### اختبار 2: المتجر المفتوح - زبون عادي

```
الخطوات:
1. تسجيل الدخول بحساب زبون عادي
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المفتوح
4. تصفح المنتجات

النتيجة المتوقعة:
✅ عرض المنتجات مع الصور
✅ عرض الأزرار (إضافة للسلة، الكمية)
```

### اختبار 3: المتجر المغلق - صاحب المتجر

```
الخطوات:
1. تسجيل الدخول بحساب صاحب المتجر المغلق
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المغلق الخاص بك
4. تصفح المنتجات

النتيجة المتوقعة:
✅ عرض المنتجات مع الصور (لأنك صاحب المتجر)
✅ عرض الأزرار (إضافة للسلة، الكمية)
```

### اختبار 4: المتجر المغلق - زبون مسجل

```
الخطوات:
1. تسجيل الدخول بحساب زبون مسجل في المتجر المغلق
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المغلق
4. تصفح المنتجات

النتيجة المتوقعة:
⚠️ عرض المنتجات كنصوص فقط بدون صور (🎯 هذا هو التعديل الرئيسي)
✅ عرض اسم المنتج، السعر، الكمية، وصف المتجر
✅ عرض الأزرار (إضافة للسلة، الكمية)
```

### اختبار 5: المتجر المغلق - زبون غير مسجل

```
الخطوات:
1. تسجيل الدخول بحساب زبون غير مسجل
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المغلق

النتيجة المتوقعة:
❌ رسالة "الدخول مقيد"
   - اسم المتجر
   - الرسالة: حسابك غير مسجل في قائمة الزبائن الآجلين
   - زر التواصل مع البائع
❌ عدم عرض المنتجات على الإطلاق
```

### اختبار 6: المتجر المغلق - عرض الفئات

```
الخطوات:
1. تسجيل الدخول بحساب زبون مسجل في متجر مغلق يحتوي على فئات
2. دخول "تصفح المتاجر 🛍️"
3. اختيار المتجر المغلق
4. اختيار فئة معينة

النتيجة المتوقعة:
⚠️ عرض منتجات الفئة كنصوص فقط بدون صور
✅ عرض اسم الفئة واسم المتجر
✅ عرض الأزرار (إضافة للسلة، الكمية)
```

---

## 🔍 نقاط المراقبة الرئيسية

| النقطة | المتوقع | كيفية التحقق |
|--------|--------|-------------|
| **صور في المتجر المفتوح** | ✅ ظاهرة | تشغيل الاختبار 1 & 2 |
| **صور للمالك في المتجر المغلق** | ✅ ظاهرة | تشغيل الاختبار 3 |
| **بدون صور للزبون المسجل** | ❌ غير ظاهرة | تشغيل الاختبار 4 |
| **رسالة رفض للزبون غير المسجل** | ✅ ظاهرة | تشغيل الاختبار 5 |
| **نفس المنطق في الفئات** | ❌ بدون صور | تشغيل الاختبار 6 |

---

## 📊 جدول النتائج

استخدم هذا الجدول لتسجيل نتائج الاختبارات:

```
الاختبار | الوصف | النتيجة | ملاحظات
---------|--------|----------|----------
1 | متجر مفتوح - صاحب | ✅/❌ | 
2 | متجر مفتوح - زبون | ✅/❌ | 
3 | متجر مغلق - صاحب | ✅/❌ | 
4 | متجر مغلق - زبون مسجل | ✅/❌ | 
5 | متجر مغلق - زبون غير مسجل | ✅/❌ | 
6 | متجر مغلق - عرض الفئات | ✅/❌ | 
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: الزبون المسجل يرى الصور في متجر مغلق

**السبب المحتمل:**
- `require_registration` غير مفعل في البيانات
- `is_store_owner` يتم حسابه بشكل خاطئ
- `send_product_with_image` لا يتلقى معامل `show_image`

**كيفية التصحيح:**
1. تحقق من قيمة `require_registration` في جدول `Sellers`
2. تحقق من أن `customer_telegram_id` و `seller_telegram_id` يتم تمريرهما بشكل صحيح
3. تحقق من سجلات البوت (Logs) للأخطاء

### المشكلة: جميع المنتجات تظهر بدون صور حتى في متجر مفتوح

**السبب المحتمل:**
- `require_registration` يتم حسابه بشكل خاطئ
- منطق `show_image` معكوس

**كيفية التصحيح:**
1. تحقق من قيم `require_registration` للمتاجر المختلفة
2. تحقق من منطق الحساب في `send_store_catalog_by_telegram_id`

### المشكلة: خطأ في التطبيق عند تصفح المتجر

**السبب المحتمل:**
- خطأ في الكود الجديد
- مشكلة في قاعدة البيانات

**كيفية التصحيح:**
1. اطلب السجلات (Logs) من البوت
2. تحقق من رسالة الخطأ
3. تصحح الخطأ في `bot.py`

---

## 📝 مثال على الاختبار اليدوي

```python
# يمكنك تشغيل هذا الكود لاختبار منطق العرض
require_registration = 1  # متجر مغلق
is_store_owner = False  # زبون (ليس مالك)

show_image = not require_registration or is_store_owner
# النتيجة: show_image = False ✅

print(f"show_image = {show_image}")  # يطبع: False
```

---

## ✅ قائمة التحقق النهائية

قبل إطلاق التعديلات على الإنتاج:

- [ ] تم اختبار جميع 6 حالات اختبار
- [ ] جميع النتائج متطابقة مع المتوقع
- [ ] لا توجد أخطاء في السجلات (Logs)
- [ ] تم التحقق من متجر مفتوح واحد على الأقل
- [ ] تم التحقق من متجر مغلق واحد على الأقل
- [ ] تم اختبار مع حسابات مختلفة
- [ ] الأداء مقبول (لا توجد تأخيرات)
- [ ] تم توثيق أي مشاكل وجدت

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تحقق من السجلات (Logs) في البوت
2. تحقق من جدول `Sellers` والقيم الفعلية
3. راجع ملف `FINAL_RESTRICTED_STORE_SUMMARY.md` للشرح التفصيلي
4. تشغيل `test_restricted_store_changes.py` للتحقق من المنطق

---

**آخر تحديث:** [التاريخ الحالي]
