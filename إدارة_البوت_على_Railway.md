# إدارة البوت على Railway

## 📋 معلومات البوت على Railway

البوت يعمل حالياً على منصة **Railway** في السحابة، مما يعني أنه يعمل 24/7 بدون الحاجة لتشغيله محلياً.

---

## 🔧 الملفات المهمة للـ Railway:

### 1. **Procfile**
```
worker: python bot.py
```
- يخبر Railway كيف يشغل البوت
- `worker` هو نوع الخدمة (يعمل بشكل مستمر)

### 2. **runtime.txt**
```
python-3.12.3
```
- يحدد إصدار Python المستخدم

### 3. **requirements.txt**
- يحتوي على جميع المكتبات المطلوبة
- Railway يثبتها تلقائياً عند النشر

---

## 🚀 كيفية تحديث البوت على Railway:

### الطريقة 1: استخدام ملف `.bat` (الأسهل)
```powershell
.\deploy_to_cloud.bat
```

هذا الملف يقوم بـ:
1. إضافة جميع الملفات إلى Git
2. حفظ التغييرات (Commit)
3. رفع التغييرات إلى Railway (Push)

### الطريقة 2: يدوياً
```powershell
# 1. إضافة الملفات
git add .

# 2. حفظ التغييرات
git commit -m "وصف التغييرات"

# 3. رفع إلى Railway
git push origin main
```

---

## 📊 متغيرات البيئة على Railway:

يجب أن تكون هذه المتغيرات موجودة في إعدادات Railway:

```
TELEGRAM_BOT_TOKEN=8562406465:AAEudEf_n6ZDnMlVy_UTwJ7eG1hBdDb1Tgw
DATABASE_URL=postgresql://postgres:bqcTJxNXLgwOftDoarrtmjmjYWurEIEh@switchback.proxy.rlwy.net:20266/railway
BOT_ADMIN_ID=1041977029
```

### كيفية إضافة/تعديل متغيرات البيئة:
1. اذهب إلى Railway Dashboard
2. اختر المشروع (Project)
3. اختر الخدمة (Service)
4. اذهب إلى **Variables** tab
5. أضف أو عدل المتغيرات

---

## 🔍 كيفية التحقق من حالة البوت:

### 1. **من Railway Dashboard:**
   - اذهب إلى [railway.app](https://railway.app)
   - سجل الدخول
   - اختر المشروع
   - تحقق من **Deployments** tab
   - يجب أن يكون آخر deployment ناجح (✅)

### 2. **من Telegram:**
   - افتح البوت في Telegram
   - أرسل `/start`
   - إذا استجاب البوت، فهو يعمل ✅

### 3. **من Logs:**
   - في Railway Dashboard
   - اذهب إلى **Logs** tab
   - يمكنك رؤية جميع رسائل `print()` والأخطاء

---

## 🛠️ حل المشاكل الشائعة:

### البوت لا يعمل:
1. **تحقق من Logs:**
   - اذهب إلى Railway → Logs
   - ابحث عن أخطاء (Errors)

2. **تحقق من المتغيرات:**
   - تأكد من وجود جميع متغيرات البيئة
   - تأكد من صحة القيم

3. **تحقق من آخر Deployment:**
   - إذا فشل آخر deployment، حاول إعادة النشر

### البوت لا يستجيب:
1. **تحقق من Token:**
   - تأكد من صحة `TELEGRAM_BOT_TOKEN`
   - يمكنك الحصول على token جديد من [@BotFather](https://t.me/BotFather)

2. **تحقق من Database:**
   - تأكد من صحة `DATABASE_URL`
   - تأكد من أن قاعدة البيانات تعمل

### تحديثات لا تظهر:
1. **انتظر قليلاً:**
   - Railway يحتاج 1-2 دقيقة لبناء وتشغيل البوت

2. **تحقق من Git:**
   - تأكد من أن التغييرات تم رفعها بنجاح
   - تحقق من آخر commit في Railway

---

## 📝 نصائح مهمة:

### ✅ **قبل النشر:**
- اختبر البوت محلياً أولاً
- تأكد من عدم وجود أخطاء في الكود
- تأكد من تحديث `requirements.txt` إذا أضفت مكتبات جديدة

### ✅ **عند النشر:**
- استخدم رسائل commit واضحة
- مثال: `git commit -m "إضافة زر حذف زبون آجل"`
- انتظر حتى يكتمل البناء (Build)

### ✅ **بعد النشر:**
- تحقق من Logs للتأكد من عدم وجود أخطاء
- اختبر البوت في Telegram
- راقب Logs لمدة دقائق للتأكد من الاستقرار

---

## 🔄 إعادة تشغيل البوت:

### من Railway Dashboard:
1. اذهب إلى الخدمة (Service)
2. اضغط على **Restart** button

### من Command Line:
```powershell
# إعادة النشر (سيتم إعادة التشغيل تلقائياً)
git commit --allow-empty -m "Restart bot"
git push origin main
```

---

## 📞 الدعم:

إذا واجهت مشاكل:
1. تحقق من Logs في Railway
2. تحقق من [Railway Documentation](https://docs.railway.app)
3. تحقق من [Telegram Bot API Documentation](https://core.telegram.org/bots/api)

---

## 🎯 ملخص سريع:

```powershell
# تحديث البوت على Railway:
.\deploy_to_cloud.bat

# أو يدوياً:
git add .
git commit -m "وصف التغييرات"
git push origin main
```

**انتظر 1-2 دقيقة** ثم اختبر البوت في Telegram! 🚀
