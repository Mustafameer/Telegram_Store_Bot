# 🚀 دليل تشغيل تطبيق متجر تليجرام

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. **Flutter SDK** (الإصدار 3.10.3 أو أحدث)
   - تحميل من: https://flutter.dev/docs/get-started/install
   - التحقق من التثبيت: `flutter doctor`

2. **Android Studio** (للتطوير على Android)
   - تحميل من: https://developer.android.com/studio
   - تثبيت Android SDK و Android Emulator

3. **VS Code أو Android Studio** (محرر الكود)

---

## 🔧 خطوات الإعداد الأولي

### الخطوة 1: فتح المشروع

1. افتح **Android Studio** أو **VS Code**
2. افتح المجلد: `C:\Users\Hp\Desktop\TelegramStoreBot\flutter_store_app`

### الخطوة 2: تثبيت المكتبات

افتح Terminal في Android Studio أو Command Prompt واكتب:

```bash
cd flutter_store_app
flutter pub get
```

هذا الأمر سيقوم بتحميل جميع المكتبات المطلوبة.

### الخطوة 3: التحقق من إعدادات Flutter

```bash
flutter doctor
```

تأكد من أن جميع العناصر تظهر بعلامة ✅

---

## 📱 تشغيل التطبيق على Android

### الطريقة 1: على محاكي Android (Emulator)

1. **إنشاء محاكي Android:**
   - افتح Android Studio
   - اذهب إلى: **Tools → Device Manager**
   - اضغط **Create Device**
   - اختر جهاز (مثل Pixel 5)
   - اختر نظام Android (API 30 أو أحدث)
   - اضغط **Finish**

2. **تشغيل المحاكي:**
   - من Device Manager، اضغط زر ▶️ بجانب الجهاز

3. **تشغيل التطبيق:**
   ```bash
   flutter run
   ```
   أو
   ```bash
   flutter run -d android
   ```

### الطريقة 2: على جهاز Android حقيقي

1. **تفعيل وضع المطور:**
   - اذهب إلى **الإعدادات → حول الهاتف**
   - اضغط على **رقم الإصدار** 7 مرات
   - سترى رسالة "أصبحت مطوراً"

2. **تفعيل USB Debugging:**
   - اذهب إلى **الإعدادات → خيارات المطور**
   - فعّل **USB Debugging**

3. **ربط الهاتف:**
   - وصّل الهاتف بالكمبيوتر عبر USB
   - امنح الإذن على الهاتف عند ظهور النافذة

4. **تشغيل التطبيق:**
   ```bash
   flutter run
   ```
   Flutter سيكتشف الجهاز تلقائياً

---

## 🍎 تشغيل التطبيق على iOS (macOS فقط)

1. **تثبيت Xcode:**
   - من App Store على Mac

2. **تشغيل التطبيق:**
   ```bash
   flutter run -d ios
   ```

---

## 💻 تشغيل التطبيق على Windows (Desktop)

```bash
flutter run -d windows
```

---

## 🏗️ بناء ملف APK للتثبيت على Android

لإنشاء ملف APK يمكن تثبيته على أي جهاز Android:

```bash
flutter build apk --release
```

الملف سيكون في:
```
flutter_store_app\build\app\outputs\flutter-apk\app-release.apk
```

يمكنك نسخ هذا الملف إلى هاتفك وتثبيته مباشرة.

---

## 🔍 حل المشاكل الشائعة

### المشكلة: `flutter: command not found`
**الحل:** تأكد من إضافة Flutter إلى PATH:
- Windows: أضف `C:\flutter\bin` إلى متغيرات البيئة

### المشكلة: لا يوجد أجهزة متصلة
**الحل:**
```bash
flutter devices
```
إذا لم يظهر أي جهاز:
- تأكد من تشغيل المحاكي
- أو وصّل جهاز Android وفعّل USB Debugging

### المشكلة: أخطاء في المكتبات
**الحل:**
```bash
flutter clean
flutter pub get
flutter run
```

### المشكلة: قاعدة البيانات لا تعمل
**الحل:** تأكد من وجود مجلد `data` في المجلد الرئيسي:
```
TelegramStoreBot\data\store_local_new.db
```

---

## 📝 ملاحظات مهمة

1. **قاعدة البيانات:** التطبيق يستخدم نفس قاعدة البيانات الموجودة في `TelegramStoreBot\data\store_local_new.db`

2. **المزامنة:** التطبيق يقوم بمزامنة تلقائية كل 15 دقيقة مع السحابة

3. **تسجيل الدخول:** استخدم معرف Telegram الخاص بك (مثل: 1041977029)

4. **الوضع الداكن:** التطبيق يدعم الوضع الداكن تلقائياً حسب إعدادات الجهاز

---

## 🎯 أوامر سريعة

```bash
# عرض الأجهزة المتاحة
flutter devices

# تشغيل على جهاز محدد
flutter run -d <device-id>

# بناء APK
flutter build apk --release

# بناء App Bundle (لـ Google Play)
flutter build appbundle --release

# تنظيف المشروع
flutter clean

# تحديث المكتبات
flutter pub upgrade
```

---

## ✅ التحقق من نجاح التشغيل

عند تشغيل التطبيق بنجاح، سترى:

1. شاشة تسجيل الدخول
2. حقل لإدخال Telegram ID
3. زر "دخول"
4. شريط في الأسفل يعرض حالة المزامنة

---

## 🆘 الحصول على المساعدة

إذا واجهت أي مشكلة:
1. تحقق من `flutter doctor`
2. راجع ملف `MOBILE_README.md`
3. تحقق من الأخطاء في Terminal

---

**بالتوفيق! 🎉**
