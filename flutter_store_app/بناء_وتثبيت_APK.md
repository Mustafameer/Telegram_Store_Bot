# 📦 دليل بناء وتثبيت APK على الموبايل

## 🎯 الهدف: إنشاء ملف APK يمكن تثبيته على أي هاتف Android

---

## 📝 خطوات البناء:

### 1. بناء ملف APK للتثبيت

افتح Terminal في مجلد المشروع واكتب:

```bash
cd flutter_store_app
flutter build apk --release
```

هذا الأمر سيستغرق بضع دقائق في المرة الأولى.

---

## 📍 مسار ملف APK بعد البناء:

بعد اكتمال البناء، سيكون ملف APK في:

```
flutter_store_app\build\app\outputs\flutter-apk\app-release.apk
```

**المسار الكامل:**
```
C:\Users\Hp\Desktop\TelegramStoreBot\flutter_store_app\build\app\outputs\flutter-apk\app-release.apk
```

---

## 📤 كيفية إرسال الملف:

### الطريقة 1: عبر USB
1. انسخ الملف `app-release.apk` إلى الموبايل
2. افتح الملف على الموبايل
3. فعّل "تثبيت من مصادر غير معروفة" إذا طُلب منك
4. اضغط "تثبيت"

### الطريقة 2: عبر البريد الإلكتروني
1. أرسل الملف `app-release.apk` كملف مرفق
2. افتح البريد على الموبايل
3. حمّل الملف
4. اضغط عليه للتثبيت

### الطريقة 3: عبر التخزين السحابي
1. ارفع الملف إلى Google Drive / Dropbox / OneDrive
2. شارك الرابط
3. حمّل الملف على الموبايل
4. ثبّت التطبيق

### الطريقة 4: عبر Bluetooth
1. أرسل الملف عبر Bluetooth
2. اقبل الملف على الموبايل
3. افتح الملف للتثبيت

### الطريقة 5: عبر WhatsApp / Telegram
1. أرسل الملف عبر WhatsApp أو Telegram
2. حمّل الملف على الموبايل
3. ثبّت التطبيق

---

## 🔧 بناء APK محسّن (أصغر حجماً):

### APK واحد (للأجهزة الحديثة فقط):
```bash
flutter build apk --release
```

### APK منفصل لكل معمارية (أصغر حجماً):
```bash
flutter build apk --split-per-abi --release
```

هذا سينشئ 3 ملفات:
- `app-armeabi-v7a-release.apk` (للأجهزة القديمة)
- `app-arm64-v8a-release.apk` (للأجهزة الحديثة - الأكثر استخداماً)
- `app-x86_64-release.apk` (للمحاكيات)

**استخدم `app-arm64-v8a-release.apk` لمعظم الأجهزة الحديثة.**

---

## 📊 حجم الملف:

- **APK كامل**: ~50-80 MB
- **APK منفصل (arm64)**: ~20-30 MB (أصغر)

---

## ⚠️ ملاحظات مهمة:

### 1. تفعيل "تثبيت من مصادر غير معروفة"
على الموبايل المستهدف:
- اذهب إلى: **الإعدادات → الأمان**
- فعّل **"تثبيت من مصادر غير معروفة"** أو **"Install unknown apps"**

### 2. التوقيع
- APK الذي تبنيّه غير موقّع للتوزيع العام
- مناسب للاستخدام الشخصي أو داخل المؤسسة
- للتوزيع على Google Play، تحتاج توقيع رسمي

### 3. الأمان
- تأكد من مصدر الملف قبل التثبيت
- استخدم قنوات آمنة لنقل الملف

---

## 🚀 خطوات سريعة:

```bash
# 1. انتقل لمجلد المشروع
cd flutter_store_app

# 2. نظف المشروع (اختياري)
flutter clean

# 3. ثبّت المكتبات
flutter pub get

# 4. ابني APK
flutter build apk --release

# 5. ابحث عن الملف في:
# build\app\outputs\flutter-apk\app-release.apk
```

---

## 📱 تثبيت APK على الموبايل:

1. **انسخ الملف** إلى الموبايل (USB/Email/Cloud)
2. **افتح الملف** من مدير الملفات
3. **اقبل التثبيت** إذا طُلب منك
4. **افتح التطبيق** بعد التثبيت

---

## ✅ التحقق من نجاح البناء:

بعد البناء، يجب أن ترى:
```
✓ Built build\app\outputs\flutter-apk\app-release.apk (XX.XMB)
```

---

## 🎯 ملخص المسار:

**ملف APK النهائي:**
```
C:\Users\Hp\Desktop\TelegramStoreBot\flutter_store_app\build\app\outputs\flutter-apk\app-release.apk
```

**يمكنك نسخ هذا الملف وإرساله لأي موبايل Android للتثبيت!** 📲
