# 📦 الفرق بين Debug APK و Release APK

## 📍 المسار الصحيح (كما في الصورة):

```
flutter_store_app\build\app\outputs\flutter-apk\
```

**الملفات الموجودة:**
- `app-debug.apk` ✅ (موجود حالياً)
- `app-debug.apk.sha1` (ملف التحقق)

---

## 🔍 الفرق بين Debug و Release:

### 📱 **app-debug.apk** (الإصدار التجريبي)
- ✅ **الحجم**: أصغر قليلاً
- ✅ **السرعة**: أسرع في البناء
- ⚠️ **الأداء**: أبطأ قليلاً في التشغيل
- ⚠️ **الحجم النهائي**: أكبر عند التثبيت
- ⚠️ **الأمان**: أقل أماناً (يحتوي على معلومات التطوير)
- ✅ **الاستخدام**: للتطوير والاختبار فقط

### 🚀 **app-release.apk** (الإصدار النهائي)
- ✅ **الحجم**: محسّن وأصغر
- ✅ **الأداء**: أسرع وأكثر كفاءة
- ✅ **الأمان**: أكثر أماناً (مُحسّن للإنتاج)
- ✅ **الاستخدام**: للتوزيع والتثبيت على أجهزة المستخدمين
- ✅ **الاستقرار**: أكثر استقراراً

---

## 💡 أي واحد يجب استخدامه؟

### ✅ للتثبيت على موبايل آخر: استخدم **Release**

```bash
cd flutter_store_app
flutter build apk --release
```

**الملف سيكون:**
```
build\app\outputs\flutter-apk\app-release.apk
```

### ⚠️ Debug APK (الموجود حالياً):
- يمكن استخدامه للتجربة السريعة
- **لكن** Release أفضل للتوزيع

---

## 🎯 خطوات بناء Release APK:

### الطريقة 1: APK كامل (موصى به)
```bash
cd flutter_store_app
flutter build apk --release
```

**النتيجة:**
- `app-release.apk` (~50-80 MB)
- يعمل على جميع الأجهزة

### الطريقة 2: APK منفصل (أصغر حجماً)
```bash
cd flutter_store_app
flutter build apk --split-per-abi --release
```

**النتيجة:**
- `app-arm64-v8a-release.apk` (~20-30 MB) ← **استخدم هذا**
- `app-armeabi-v7a-release.apk` (للأجهزة القديمة)
- `app-x86_64-release.apk` (للمحاكيات)

---

## 📤 إرسال الملف:

### من الصورة، الملف الحالي:
```
build\app\outputs\flutter-apk\app-debug.apk
```

### بعد بناء Release:
```
build\app\outputs\flutter-apk\app-release.apk
```

**يمكنك نسخ أي من الملفين وإرسالهما للموبايل!**

---

## ✅ الخلاصة:

| الميزة | Debug APK | Release APK |
|--------|-----------|-------------|
| **الحجم** | أكبر | أصغر |
| **الأداء** | أبطأ | أسرع |
| **الاستخدام** | للتطوير | للتوزيع |
| **موصى به** | ❌ | ✅ |

**للتثبيت على موبايل آخر: استخدم `app-release.apk`** 🎯
