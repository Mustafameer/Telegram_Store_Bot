# كيفية تشغيل Python في VS Code

## الطرق المختلفة لتشغيل Python في VS Code:

### 1. **تشغيل مباشر من Terminal (الأسهل)**
   - افتح Terminal في VS Code: `Ctrl + ~` (أو Terminal → New Terminal)
   - اكتب الأمر:
     ```bash
     python bot.py
     ```
   - أو استخدم ملف `.bat` الموجود:
     ```bash
     start_bot.bat
     ```

### 2. **تشغيل من Run Button (زر التشغيل)**
   - افتح ملف `bot.py`
   - اضغط على زر **▶ Run Python File** في الزاوية اليمنى العلوية
   - أو اضغط `Ctrl + F5`

### 3. **تشغيل مع Debug (للتشخيص)**
   - افتح ملف `bot.py`
   - اضغط `F5` أو اذهب إلى **Run → Start Debugging**
   - اختر **"تشغيل البوت (bot.py)"** من القائمة المنسدلة
   - يمكنك وضع نقاط توقف (Breakpoints) بالنقر على يسار رقم السطر

### 4. **تشغيل من Command Palette**
   - اضغط `Ctrl + Shift + P`
   - اكتب: `Python: Run Python File in Terminal`
   - اضغط Enter

### 5. **تشغيل من Explorer (قائمة الملفات)**
   - انقر بزر الماوس الأيمن على ملف `bot.py`
   - اختر **"Run Python File in Terminal"**

## ملاحظات مهمة:

### ✅ **المتطلبات:**
1. تأكد من تثبيت Python على جهازك
2. تأكد من تثبيت امتداد Python في VS Code:
   - اضغط `Ctrl + Shift + X`
   - ابحث عن "Python" من Microsoft
   - اضغط Install

### 🔧 **إعدادات VS Code:**
- تم إصلاح ملف `.vscode/launch.json` ليشمل إعدادات لتشغيل `bot.py` مباشرة
- تم إضافة متغيرات البيئة (Environment Variables) في إعدادات Debug

### 📝 **نصائح:**
- استخدم **Terminal** للتشغيل العادي (الأسرع)
- استخدم **Debug** عند الحاجة لتشخيص الأخطاء أو وضع نقاط توقف
- يمكنك رؤية رسائل `print()` في Terminal أو Debug Console

### ⚠️ **حل مشكلة `${file}`:**
- إذا ظهرت رسالة خطأ عن `${file}`، تأكد من فتح ملف Python أولاً قبل الضغط على Run
- أو استخدم إعداد **"تشغيل البوت (bot.py)"** الذي يعمل مباشرة بدون فتح ملف

## الأوامر السريعة:
- `Ctrl + F5`: تشغيل بدون Debug
- `F5`: تشغيل مع Debug
- `Ctrl + ~`: فتح/إغلاق Terminal
- `Ctrl + Shift + P`: فتح Command Palette
