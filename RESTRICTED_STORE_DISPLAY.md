# تعديل عرض المنتجات في المتاجر المغلقة - بوت التليجرام

## المشكلة الأصلية
في تطبيق الدسكتوب (Flutter):
- في المتاجر المغلقة (`require_registration = 1`): 
  - صاحب المتجر يرى بطاقات المنتجات كاملة **مع الصور**
  - الزبائن يرون بطاقات المنتجات **بدون صور** (صورة بحجم 4.97 × 4.97)

في بوت التليجرام **قبل التعديل**:
- الزبائن كانوا يرون بطاقات المنتجات **مع الصور** دائماً

## الحل المطبق
تم تعديل منطق عرض المنتجات في بوت التليجرام ليطابق سلوك تطبيق الدسكتوب:
- الزبائن في المتاجر المغلقة: **لا يرون الصور** (صورة نصية فقط)
- صاحب المتجر في متجره الخاص: **يرى الصور كاملة**

## التعديلات في bot.py

### 1. تعديل دالة `send_product_with_image` (السطر 3125)
**من:**
```python
def send_product_with_image(chat_id, product, markup=None, seller_name=""):
```

**إلى:**
```python
def send_product_with_image(chat_id, product, markup=None, seller_name="", show_image=True):
    """إرسال منتج مع صورته
    
    Args:
        chat_id: معرف الدردشة
        product: بيانات المنتج
        markup: أزرار إضافية
        seller_name: اسم المتجر
        show_image: هل نعرض الصورة (False للمتاجر المغلقة)
    """
```

عند `show_image=False`:
- يتم إرسال رسالة نصية فقط بدون محاولة جلب أو إرسال الصورة
- الرسالة تحتوي على: اسم المنتج، السعر، الكمية المتاحة، اسم المتجر

### 2. تعديل دالة `send_store_catalog_by_telegram_id` (السطر 7800)
**المنطق:**
```python
# في المتاجر المغلقة: عدم عرض الصور للزبائن (ما عدا صاحب المتجر)
is_store_owner = (customer_telegram_id == seller_telegram_id) if customer_telegram_id else True
show_image = not require_registration or is_store_owner

send_product_with_image(chat_id, product, markup, store_name, show_image=show_image)
```

**شرح المنطق:**
- إذا كان المتجر يتطلب تسجيل (`require_registration = 1`):
  - صاحب المتجر يرى الصور: `show_image = True`
  - الزبائن الآخرون لا يرون الصور: `show_image = False`
- إذا كان المتجر مفتوحاً (`require_registration = 0`):
  - الجميع يرون الصور: `show_image = True`

### 3. تعديل دالة `handle_view_category` (السطر 8071)
**نفس المنطق عند اختيار قسم معين:**
```python
# في المتاجر المغلقة: عدم عرض الصور للزبائن (ما عدا صاحب المتجر)
is_store_owner = (customer_telegram_id == seller[1])  # seller[1] هو TelegramID

show_image = not require_registration or is_store_owner

send_product_with_image(call.message.chat.id, product, markup, seller_name, show_image=show_image)
```

**الفرق من النسخة السابقة:**
- تم حذف منطق `if require_registration` الخاص
- الآن نستخدم دالة موحدة `send_product_with_image` مع معامل `show_image`
- هذا يوفر كود أنظف وأسهل للصيانة

## المسارات المتأثرة
1. **عرض المتجر مباشرة:**
   - من قائمة "تصفح المتاجر 🛍️"
   - `handle_view_store()` → `send_store_catalog_by_telegram_id()` ✅

2. **عرض منتجات قسم معين:**
   - اختيار قسم من المتجر
   - `handle_view_category()` ✅

3. **عرض منتجات بدون فئات:**
   - عندما لا توجد فئات في المتجر
   - `send_store_catalog_by_telegram_id()` (الجزء بدون فئات) ✅

## اختبار التعديلات

### حالة اختبار 1: متجر مفتوح (بدون تسجيل)
```
- صاحب المتجر: ✅ يرى الصور
- زبون: ✅ يرى الصور
```

### حالة اختبار 2: متجر مغلق (يتطلب تسجيل)
```
- صاحب المتجر: ✅ يرى الصور
- زبون غير مسجل: ❌ لا يرى الصور (رسالة "غير مسجل")
- زبون مسجل: ❌ يرى الصور كنص فقط (بدون صور)
```

### حالة اختبار 3: عرض الفئات
```
- في متجر مغلق:
  - صاحب المتجر: ✅ يرى صور المنتجات
  - زبون مسجل: ❌ يرى نص فقط بدون صور
```

## الفوائد
✅ توحيد السلوك بين تطبيق الدسكتوب وبوت التليجرام
✅ احترام إعدادات خصوصية المتجر
✅ تحسين تجربة المستخدم (أداء أفضل، استهلاك بيانات أقل للزبائن)
✅ كود أنظف وأسهل للصيانة

## ملاحظات مهمة
- المعامل `show_image` افتراضياً `True` لضمان توافقية عكسية
- البحث عن المنتجات والطلبات السابقة لم تتأثر بهذه التعديلات
- إذا كانت هناك مسارات أخرى لعرض المنتجات، يجب تطبيق نفس المنطق عليها
